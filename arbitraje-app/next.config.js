/** @type {import('next').NextConfig} */
const nextConfig = {
  // App mínima con servidor (NextAuth necesita rutas de API y middleware).
  // OJO: NO heredar el output:'export' del sitio principal.
};

module.exports = nextConfig;
